import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.updemy-course,ionic-ang',
  appName: 'ionic-ang-course',
  webDir: 'www'
};

export default config;
