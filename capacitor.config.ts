import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.updemycourse.ionicang',
  appName: 'ionic-ang-course',
  webDir: 'www'
};

export default config;
